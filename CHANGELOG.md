# Changelog

本文件记录 `javadoc-mcp` 的对外发布变更。

## 1.0.5 - 2026-06-04

### 修复

- 修复 CLI 在通过符号链接目录启动时无法正确识别主模块的问题，确保 `javadoc-cli` 在全局安装、`npx` 和其他软链接包装路径下都能正常执行。

### 验证

- 新增 CLI 软链接启动回归测试，覆盖发布包常见的 `--version` 调用路径。
